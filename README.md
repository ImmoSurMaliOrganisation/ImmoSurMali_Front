# 🏠 ImmoSur Mali v1.0

## 📦 Installation
1. Extract the ZIP file.
2. Run `npm install`.
3. Start the project with `npm start`.

## URL pour acceder la page admin 

`http://localhost:4200/admin/login`
